<div
  class="modal fade"
  id="deleteModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="deleteModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content black-backend">
      <div class="modal-header">
        <i class="fas fa-trash fa-2x"></i>
        <h5 class="modal-title" id="deleteModalLabel">Silme Onayı</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Kapat"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">Bu kaydı silmek istediğinize emin misiniz?</div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">
          İptal
        </button>
        <button type="button" class="btn btn-danger" id="confirmDeleteBtn">
          Sil
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  let deleteUrl = "";
  let deleteId = "";
  let reloadId = "";

  function confirmDelete(url, id, tableId) {
    deleteUrl = url;
    deleteId = id;
    reloadId = tableId;
    $("#deleteModal").modal("show");
  }

  $(document).ready(function () {
    event.preventDefault();
    $("#confirmDeleteBtn").click(function () {
      $.ajax({
        url: deleteUrl,
        type: "DELETE",
        success: function (response) {
          showToast("Silme İşlemi", "Başarı ile gerçekleşti.");

          $(reloadId).DataTable().ajax.reload(null, false);
        },
        error: function (xhr, status, error) {
          showToast("Silme İşlemi", "Başarısız oldu." + xhr.responseText);
        },
      });

      $("#deleteModal").modal("hide");
    });
  });
</script>
